image: docker:latest

pipelines:
  default:
    - step:
        name: "1. Copying .env file."
        script:
        - cp .env.dist .env
    - step:
        name: "2. Saying hello."
        script:
        - echo "Hello"
    - step:
        name: "3. Docker version."
        script:
          - docker version
        services:
        - docker
        image: php:8.1
        caches:
        - composer
        # script:
        # - docker-compose up -d
    # - step:
    #     script:
    #     - docker-compose exec -T --user=root php bash -c 'chown -R www-data:www-data /var/www'

options:
  docker: true