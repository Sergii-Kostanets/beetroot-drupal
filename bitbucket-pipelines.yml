image: docker:stable

options:
  docker: true

pipelines:
  default:
    - step:
        name: "Test"
        script:
          - sh ci/dependencies.sh
          - docker-compose run -T client /usr/local/src/test.sh
          - docker-compose down