image: atlassian/default-image:latest

pipelines:
  default:
    - step:
        name: "1"
        script:
        - apt-get update
        - docker compose version
    - step:
        name: "2"
        script:
        - docker-compose up -d

options:
  docker: true