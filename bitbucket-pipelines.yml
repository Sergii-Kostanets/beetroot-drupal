image: ubuntu:latest

pipelines:
  default:
    - step:
        script:
          - pip install docker-compose
    - step:
        script:
          - echo "Hello"
    - step:
        script:
        - docker-compose up -d