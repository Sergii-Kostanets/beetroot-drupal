/**
 * @file
 * Beetroot Example Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.beetrootExampleTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
