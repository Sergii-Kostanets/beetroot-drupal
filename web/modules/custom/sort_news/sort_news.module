<?php

/**
 * Implements hook_theme().
 */
function sort_news_theme($existing, $type, $theme, $path): array {
  return [
    'sort_template' => [
      'variables' => ['test_var' => NULL],
    ],
  ];
}
